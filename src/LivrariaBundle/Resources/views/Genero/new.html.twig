{% extends 'base.html.twig' %}

{% block body %}
    <h1>Novo Gênero</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success pull-right" type="submit" value="Cadastrar">Cadastrar</button>
    {{ form_end(form) }}
        <a href="{{ path('genero_index') }}"><button class="btn btn-primary pull-right" style="margin-right: 5px;">Listar</button></a>
{% endblock %}
